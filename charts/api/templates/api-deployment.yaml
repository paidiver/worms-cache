apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "worms-cache-api.fullname" . }}
  labels:
    {{- include "worms-cache-api.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      component: {{ .Values.api.componentName }}
      {{- include "worms-cache-api.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        component: {{ .Values.api.componentName }}
        {{- include "worms-cache-api.labels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
        - name: wait-for-postgres
          image: "{{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}"
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          command:
            - sh
            - -ec
            - |
              echo "Waiting for PostgreSQL..."
              until pg_isready \
                -h $POSTGRES_HOST \
                -p $POSTGRES_PORT \
                -U $POSTGRES_USER; do
                sleep 2
              done
          env:
            {{- include "worms-cache-api.dbEnv" . | nindent 12 }}
          resources:
            requests:
              cpu: "20m"
              memory: "64Mi"
            limits:
              cpu: "50m"
              memory: "128Mi"
      containers:
        - name: api
          image: "{{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}"
          imagePullPolicy: {{ .Values.api.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.api.port }}
          env:
            {{- include "worms-cache-api.dbEnv" . | nindent 12 }}
            - name: DJANGO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.api.env.djangoSecretKey.secretName }}
                  key: {{ .Values.api.env.djangoSecretKey.secretKey }}
            - name: DJANGO_DEBUG
              value: "{{ .Values.api.env.djangoDebug }}"
            - name: DJANGO_PATH_PREFIX
              value: {{ default .Values.api.env.djangoPathPrefix "" | quote }}
            - name: DJANGO_ALLOWED_HOSTS
              value: localhost,127.0.0.1,0.0.0.0,{{ .Values.ingress.host }}
            - name: DJANGO_CORS_ALLOWED_ORIGINS
              value: "{{ .Values.api.env.djangoCorsAllowedOrigins }}"
            - name: DJANGO_CORS_ALLOW_ALL
              value: "{{ .Values.api.env.djangoCorsAllowAll }}"
            - name: TAXAMATCH_URL
              value: {{ default (printf "http://%s-taxamatch-service:8080" .Release.Name) .Values.api.env.taxamatchUrl | quote }}
            - name: WORMS_API_BASE_URL
              value: "{{ .Values.api.env.wormsApiBaseUrl }}"
            - name: INGEST_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.api.env.ingestApiToken.secretName }}
                  key: {{ .Values.api.env.ingestApiToken.secretKey }}
          command:
            - sh
            - -c
            - |
              poetry run gunicorn config.wsgi:application -w {{ .Values.api.workers }} -b 0.0.0.0:{{ .Values.api.port }} --timeout {{ .Values.api.timeout }}
          securityContext:
            {{- toYaml .Values.api.securityContext | nindent 12 }}
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
      imagePullSecrets:
        - name: worms-cache-api-ghcr-pull-secret
